# Codex Memory Note Template

Use this durable memory note format when the workflow itself changes:

- change: what rule or operating behavior changed
- reason: why it changed
- future default: what future sessions must now do
- scope: where the rule applies
- exceptions: where it does not apply
